# Vorschau der Solvera-Sales-Website

Dieser Branch enthält ausschließlich die Vorschau als einzelne HTML-Datei.
Er wird über GitHub Pages ausgeliefert und ist bewusst für Suchmaschinen
gesperrt (`robots.txt` und `noindex`).

Der Quellcode der Website liegt im Branch `claude/solvera-sales-website-7zt0h0`.
Neu erzeugen mit `python3 tools/vorschau-bauen.py`.
